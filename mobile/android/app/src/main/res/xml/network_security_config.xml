<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext HTTP is permitted ONLY for local dev backends:
    - 10.0.2.2  → host machine from Android emulator
    - localhost → adb reverse tcp:8080 tcp:8080 on real device
    - 127.0.0.1 → same as above
  All other domains stay HTTPS-only (Android default).
  Production BFF (https://mobile.pangkalpinangkota.go.id) is unaffected.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
