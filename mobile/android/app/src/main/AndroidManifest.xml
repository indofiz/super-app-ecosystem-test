<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:label="Smart App Test"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- DO NOT add an intent-filter for the OAuth scheme here.
                 The `appAuthRedirectScheme` placeholder in build.gradle.kts
                 already registers `net.openid.appauth.RedirectUriReceiverActivity`
                 to capture the deeplink. Putting one on MainActivity too will
                 hijack the redirect (Flutter's deeplink handling routes the
                 URL into GoRouter instead of letting AppAuth complete). -->
        </activity>
        <!-- TODO(C-04): replace custom scheme with verified App Links once
             mobile.pangkalpinangkota.go.id is live and assetlinks.json is
             published. Until then the scheme below is registered by the
             appAuthRedirectScheme placeholder in build.gradle.kts. -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>

        <!-- Required by flutter_appauth on Android 11+ (API 30+) so the
             package manager can discover (a) any installed browser that
             handles VIEW+BROWSABLE for https (b) any provider of the Custom
             Tabs service. Without these, AppAuth's internal browser
             discovery returns empty under the package-visibility rules and
             the auth flow ends with "User Cancelled flow" before the browser
             ever opens. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <category android:name="android.intent.category.BROWSABLE"/>
            <data android:scheme="https"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <category android:name="android.intent.category.BROWSABLE"/>
            <data android:scheme="http"/>
        </intent>
        <intent>
            <action android:name="android.support.customtabs.action.CustomTabsService"/>
        </intent>
    </queries>
</manifest>
