# env / secrets
.env
.env.local
.env.*.local

# TLS material — never commit private keys or issuer chains. The
# nginx/certs/ directory is the bind-mount target for nginx's TLS overlay.
nginx/certs/*
!nginx/certs/.gitkeep

# OS
.DS_Store
Thumbs.db

# editor
.vscode/
.idea/
